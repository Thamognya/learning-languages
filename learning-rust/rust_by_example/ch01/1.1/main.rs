fn main() {
    println!("Hello, world!\n I am a rust user");
    // comments
    /*
     * comments
     * comments
     * */
    // //! library docs
    let x = 5 + /* 90 + */ 5;
    println!("Is `x` 10 or 100?\nx = {}", x); // {} is like %s in printf in c
}
